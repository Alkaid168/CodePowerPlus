# 项目协作约定

开始工作时阅读 `PROJECT_MEMORY.md`、`task_plan.md`、`findings.md` 和 `progress.md`。这些文件是长期事实记录；最新用户说明优先于旧记录。将历史事实、已验证状态、假设和待办分开，不能将计划视为已实现。

用户是北京交通大学人工智能专业本科三年级学生、ACM 校队成员和项目负责人。竞赛基础强，工程和面向对象基础薄弱。代理负责实际编码，不要求用户写代码；关键设计、技术栈与取舍使用中文和具体例子解释，帮助用户成长。按已授权范围持续完成，不反复请求继续授权。

保留用户已有改动。API 密钥只从环境配置读取。开发与学校 OJ 解耦，不假设有源码、管理员权限或私有数据。测试隔离数据库，不能污染实际学习记录。人工评审与机器产生的示例数据必须区分，实验结论必须说明数据来源和限制。

完成有意义的阶段后更新进度、发现与关键决策。个人联系方式、密钥和完整聊天截图不写入公共仓库。

## 前端 UI 强制参考

任何前端 UI 设计、实现或审查任务，必须先完整阅读 `docs/design/FINAL_FRONTEND_TEMPLATE.md`，并以其“最终版要求”为权威基线。该文件与 `docs/design/` 的历史说明冲突时，以最终模板为准。默认只实现样式与视觉结构，不新增业务逻辑，除非用户明确要求。
