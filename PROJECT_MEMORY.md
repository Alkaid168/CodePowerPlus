# 项目长期记忆

最后更新：2026-09-14。记录来源是用户陈述、原始项目材料或实际代码验证，不依赖会话自动摘要。

## 用户与协作偏好

- 用户王禹凡，北京交通大学人工智能专业本科三年级，ACM 校队成员，本项目负责人。
- 前两年主要投入竞赛；工程基础及面向对象知识较弱。时间有限，希望代理承担几乎所有编码，不布置手写代码任务。
- 希望边开发边学：关键思路、模块职责、工程决策，以及 Docker、Nginx 等技术，用通俗中文解释。无需逐行讲所有代码。
- 本会话长期使用。主动用文件记录背景、偏好、进度、历史事件、要求和探索结果。
- 本轮明确要求完整完成知识体系建设的十一项工作，之后结束本轮回复；不在中途仅交计划或反复询问是否继续。

## 项目信息

- 名称：码力加加程序设计智能辅导系统（CodePowerPlus）。
- 代码：`F:\CODES\CodePowerPlus`；GitHub：<https://github.com/Alkaid168/CodePowerPlus>。
- 资料：`F:\Alkaid\文件\大创-码力加加智能编程辅导系统`。原始申请材料保留在该目录。
- 团队：王禹凡、廖致涵、廖益博；导师/教练黄华（材料记载）。
- 用户提供的校内平台：<https://icpc.bjtu.edu.cn/contest>、<https://citel.bjtu.edu.cn/acm/>、<https://tongtong.bjtu.edu.cn/login>。用户猜测采用同一套评测系统，尚未验证。
- 没有学校 OJ 源码及特权数据。系统先独立建设，通过手工/公开数据导入，将来再做 OJ 适配。

## 历史与项目定位

- 用户陈述：2026 年四五月教练邀请其担任负责人；教练此前已希望把智能辅导用于学校 OJ。
- 本科生“启研”计划申请未成功，评审认为工程多、研究表达不足。教练认为项目包含围绕实际问题的研究，只是申请书没有充分体现。
- 用户陈述：2026 年七月转为申请大创。资料中的立项报告落款为六月，这两个日期不擅自合并。
- 用户说此前没有开工、因拿不到 OJ 源码而不知如何开始。教练解释知识体系、基于评测和刷题表现的推荐可以先做。该“零进度”描述历史起点；当前目录已经有代理辅助建立的早期原型。
- 核心研究对象：知识表示、题目标签与难度估计、提交证据到能力状态、分级辅导、基于前置知识的题目/路径推荐。
- “启研”材料周期为 2026.04—2027.04，大创报告排期为 2026.06—2027.06；当前按大创推进，中期答辩确切时间未知。

## 当前技术与边界

- FastAPI 提供 HTTP 接口，SQLite 保存本地数据，原生 HTML/CSS/JS 提供工作台，DeepSeek 是可选模型服务。
- Docker 用于把应用及依赖打包成一致的运行环境。当前本地单用户不需要 Nginx；公网 HTTPS 或反向代理时再引入。
- 知识分类树与前置知识 DAG 分离，稳定 ID 与版本关联所有题目证据。JSON 文件足以存储当前图结构，无需引入图数据库。
- 实验脚本可验证流程；人工构造的样例结果不能作为真实教学效果证明。

## 2026-09-13 阅读验证基线

- 已读取资料目录七个文件及 PPT 图片，核心源代码、测试、脚本和既有设计文档。
- 原有 25 项测试通过，但隔离验证发现提交后画像/推荐 500、前端空标签、推荐字段结构不匹配、Docker 缺少知识体系文件。
- 原知识体系 v2026.09.2 共 19 个节点（L1=6、L2=8、L3=5），大部分内容为模板化占位。
- 测试在临时副本执行，没有调用真实模型；源码和原材料未被上轮阅读操作修改。
- 后续完成情况以 `progress.md` 和验收记录为准。

## 2026-09-13 目录扩充决定

用户决定放弃仅保留一百多个节点的基线，要求一次扩充为覆盖主流 ICPC/OI 训练内容的完整知识体系。目标是可维护的主流覆盖，不声称穷尽所有研究级专题。扩充必须保留旧 ID、版本化、严格校验并同步测试。

- 2026.09.4 的扩充由脚本按模板批量生成，414 个节点里有 301 个占位节点（如“Io专题 3”），不能用于标注，已废弃并删除快照。
- 2026.09.5 共 304 节点，逐条自编内容，但仍保留讲解、难度、前置关系等字段。

## 2026-09-13 知识体系定稿：纯标签词表（v2026.09.6）

用户明确：知识体系是给题目打标签用的，节点必须是“一道题可能被标上的东西”；“编译与调试”“C++ 现代特性”这类不可能成为题目标签；数量要对齐 OI Wiki。

- 节点只保留 id、name、level、parent_id，删除定义、难度、典型算法、学习目标、常见误区、例子、参考资料、前置关系、状态等全部字段。
- 结构取自 OI Wiki（oi-wiki.org）栏目导航，2026-09-13 抓取；去掉栏目简介、赛事、工具软件与语言基础；把一个 OI Wiki 分组内的嵌套分组（莫队算法）提为独立 L2。
- 当前 2026.09.9 共 345 节点：9 L1 / 151 L2 / 185 L3。L2 既可作分支（数论 → 素数），也可直接是知识点（数学 → 快速幂）。
- 配套改动：Taxonomy/schema 简化；推荐改为“标签薄弱度 + 难度适配”；学习路径改为知识树的层级路径；评估删除前置违例指标；前端只显示名称与层级；旧快照 2026.09.2/09.3/09.5 一并删除。
- 结论：目录规模不是指标，可标注性才是；判断标准是“会有题目被标上它吗”。

## 2026-09-13 整体重写（v0.3）

用户判断此前代码质量太差，要求前端后端重新设计并实现。重写范围与结果：

- 后端分层：`db → repositories → services → api`；原先 250 行的 `main.py` 拆成 7 个路由模块，
  业务规则集中到 `services/`（analysis / review / profile / recommend / tutor）。
- 数据：新增 `origin`、`initial_status`、`taxonomy_version` 与 `schema_meta` 版本表；
  旧原型数据库自动补列，旧分析标记为 `legacy`（可读但不参与画像）。分析版本仍不可变，
  审核记录（`analysis_reviews`）保留 `original → new` 的对应关系。
- 接口契约：请求体禁止多余字段（提交接口没有“自由标签”入参）；错误统一 `{code, detail}`；
  新建资源返回 201；列表返回 `total`；新增 `GET /api/overview` 给工作台。
- 前端：删除 32KB 压缩单文件，改为 `index.html` + 3 个 CSS + 16 个 ES 模块
  （dom / api / store / router / components / tagEditor / 10 个页面），全部用 `h()` 建节点、
  不拼 `innerHTML`；图标由 `scripts/build_icons.py` 从 lucide 1.45.0 生成，不引入 npm 构建。
- 文档与工具：新增 `docs/architecture.md`、`scripts/smoke_test.py`；README、`.env.example` 更新，
  `docs/system-design.md` 改为指向 architecture.md 的归档说明。
- 验证：`pytest` 103 项通过；`node --check` 全部前端模块通过；自检脚本在临时数据库跑通
  「建题 → 标注 → 审核 → 提交 → 画像 → 路径 → 辅导」。
- 浏览器实测（本机 127.0.0.1:8000）：总览、知识树、题目分析三个页面渲染正常；
  知识树按 L1/L2 折叠展示 382 个节点，分析页能读取旧题并提示“旧版未迁移”。

## 2026-09-13 清空题库

按用户要求删除全部题目。用新增的 `scripts/reset_data.py` 执行（默认预览，`--yes` 才删除，
执行前自动备份数据库到系统临时目录）：删除 54 道题、51 条分析，备份文件
`%TEMP%\codepowerplus-backup-20260913-221821.db`。当前题库为空，知识体系（382 个节点）不受影响。

## 2026-09-13 叶子知识点介绍（v2026.09.7）

用户要求为每个叶子知识点写一段 50–200 字的介绍，并允许开子智能体。做法：把 342 个叶子节点
按知识树顺序切成 4 份，3 个子智能体各写一份、我写一份；用 `scripts/check_notes.py` 逐份校验
（键集合、长度、重复段落、套话、换行），再由 `_merge_notes.py` 合并进知识树。

- 结果：342 条介绍，长度 75–162 字，版本升到 2026.09.7，快照 `data/taxonomy_versions/2026.09.7.json`。
- 字段：叶子知识点新增 `summary`；分支节点仍然只有 id/name/level/parent_id。summary 只用于界面展示与学习参考，不参与题目标注（标签校验逻辑未变）。
- 校验：`Taxonomy.validate` 要求叶子节点带 20–400 字的 summary；测试新增“每个叶子都有 50–200 字介绍、无套话、不重复”，以及接口能返回 summary。
- 界面：知识树叶子悬停显示介绍，详情页展示正文段落。
- 过程记录：第 3 组初稿偏短（50–83 字），由新开的子智能体扩写到 75–108 字；两次 followup 消息未能送达旧智能体，改用新智能体完成。

## 2026-09-13 桌面工作台 UI 组件规范（设计阶段）

- 用户确认整体 UI 方向已通过，开始固化可复用模板库，但明确当前只做组件，不设计页面布局。
- 视觉方向：明亮通透、黑白灰主色、无渐变；圆角参照 Xept，输入和按钮 8px、卡片 12px、大面板 16px。
- 关键状态：输入框默认灰边，真实聚焦时才变黑；可编辑控件白底，禁用控件灰底；状态色只用于 AC/WA/TLE/错误/警告。
- 组件预览位于 `docs/design/reference/`；完整规则位于 `docs/design/README.md`、`foundations.md`、`components.md`、`motion.md`、`tokens.json` 和 `handoff.md`。
- 旧版 `app/static` 视觉不再作为后续前端实现依据。页面布局仍待用户后续明确。

## 2026-09-14 总览方案 A 实现

- 用户选择方案 A · Paper，要求直接实现并反复自检。
- 生产前端新增 `app/static/css/overview.css`，重写总览视图结构；全局颜色、圆角、边界、表单和卡片同步到黑白灰模板库。
- 总览使用真实 `/api/overview` 数据：题目、待审核、提交、知识节点、薄弱知识点与系统状态；没有新增业务接口或虚构队列功能。
- 验证：106 项 pytest 通过；全部 ES 模块 `node --check` 通过；10 个路由浏览器检查无控制台错误、无横向溢出；空学习者分支与减少动态模式通过。
- 总览方案 A 已实现，其余业务页面仍是旧布局结构继承新 token，后续需要逐页重做。

## 2026-09-14 总览模板全局约束与品牌更新

- LOGO.png 已去除白色背景并裁切为透明 PNG：`app/static/assets/codepowerplus-mark.png`。
- 左上角品牌改为透明 LOGO + 英文 `CodePowerPlus`；艺术字体尚未最终商定，当前使用 Avenir Next / SF Pro Rounded 回退栈。
- 左侧栏删除分组标题与底部“当前学习者 / 知识体系 / 模型状态”。
- 旧阶段曾采用“固定顶栏 + 中文大标题”方案；该方案已被 2026-09-14 的无顶栏平铺规则覆盖。
- 所有卡片加入 `card-enter` 入场动效，减少动态时切换为透明度过渡。
- 总览删除指标卡说明、建议段落、流程说明等重复小字；1440×900 下主体完整放下，无主体滚动条。
- 这套规则已写入 `docs/design/README.md` 和 `handoff.md`，作为所有后续页面的全局模板约束；当前阶段只实现样式，不新增业务功能。

## 2026-09-14 总览极简调整

- 用户反馈总览信息密度过高，进一步删除与左侧导航重复的“常用入口”，并移除“学习流程”“系统状态”。
- 指标从四项缩减为三项：题目、待审核分析、提交记录。
- 总览现只保留：顶部标题与操作、三项指标、当前建议、薄弱知识点。
- 1440×900 验证：主体 `scrollHeight == clientHeight`，无主体滚动条。

## 2026-09-14 无顶栏与平铺主体（历史阶段，已被白卡版本覆盖）

- 用户要求彻底去掉顶部栏，只保留左侧导航和主体区域；`app/static/index.html` 已移除 `topbar`。
- 页面标题进入主体：总览直接使用问候标题，其他页面由路由在主体首行渲染标题。
- 主体不再使用白色“气泡卡片”容器；内容直接铺在 `#f5f5f5` 背景上，通过留白、横向分隔线和局部竖线组织。
- 总览只保留：问候与操作、三项指标、当前建议、薄弱知识点。
- 浏览器验证：1440×900 下总览 `main scrollHeight == clientHeight`，无顶部栏、无卡片外壳、无主体滚动。


## 2026-09-14 大胆气泡版（历史阶段，黑色主卡已废弃）

- 用户要求取消顶部栏；左侧栏按 Xept 尺度放大到 260px，功能文字统一纯黑，条目高度 44px。
- 总览恢复白色气泡卡片：三张指标卡、一张黑色主建议卡、一张薄弱知识点卡。
- 主建议卡使用大胆黑白对比，标题、数字和按钮整体放大；页面在 1440×900 下继续完整铺满主体且无滚动条。
- `app/static/css/components.css` 的通用 `.card` 恢复白底、1px 灰色边界、12px 圆角和轻阴影。
- 最新规则已同步到 `docs/design/`，并覆盖之前“无卡片平铺”的阶段说明。

## 2026-09-14 无顶栏平铺定稿（历史阶段，已被白卡版本覆盖）

- 最终明确：左侧栏 260px、黑色功能文字、44px 行高；页面不保留顶部栏。
- 总览主体不做白色气泡卡片，内容直接铺在 `#f5f5f5` 背景上，用留白、横向分隔线和必要竖线组织层级。
- 页面只保留三项指标、当前建议和薄弱知识点；空数据不显示无意义的圆环。
- 1440×900 下 `mainScrollHeight == mainClientHeight`，主体无滚动条。
- 上述规则已同步到 `docs/design/README.md`、`handoff.md`、`components.md` 与设计参考图。

## 2026-09-14 总览双状态与字体定稿

- 左侧栏字体使用 XEPT 同类系统字体栈，普通导航字重降到 400，不再使用粗体；宽度保持 260px，文字纯黑。
- 总览恢复白色卡片气泡，且使用网格行高让三张指标卡、主建议卡和薄弱知识点卡恰好铺满标准桌面主体。
- 已实现“有题目”状态：中间卡片读取题目列表首项，显示标题、来源、开始练习和查看分析。
- 已保留“无题目”状态：中间卡片引导建立第一道题。
- 两种状态均验证 1440×900 无主体滚动。

## 2026-09-14 XEPT 轻量白卡定稿（最新）

- 用户明确反馈黑色主建议卡太丑；总览主卡改为白色卡面。后续进一步要求删除右侧 `#F7F7F7` 内嵌卡片和 Logo。
- 黑色只用于文字、边界、选中态和小面积反色动作，不再使用大面积黑色 Hero。
- Primary 按钮保留原有黑底白字方案，Hover 使用纯黑；Secondary 保持白底和 1px 灰边界。
- 三项指标卡、主建议卡和薄弱知识点卡继续采用 12px 圆角、细边界、低阴影和较大留白，并铺满 1440×900 主体。
- 总览主卡为单一白色横向卡片，只保留标签、标题和操作按钮；空数据与有题目状态均不再显示右侧信息区。
- 2026-09-14 后续确认：用户只要求取消黑色主卡，Primary 按钮仍恢复为黑色，不与主卡颜色同步。
- 已生成最新参考图：`docs/design/reference/overview-empty.png` 与 `overview-with-problem.png`。

## 2026-09-14 前端最终模板归档

- 用户要求把此前所有最终版要求、设计规范和当前总览样式整理成后续对话可复用模板。
- 权威文件：`docs/design/FINAL_FRONTEND_TEMPLATE.md`。与 `docs/design/` 的历史说明冲突时，以该文件为准。
- 模板覆盖：浏览器全屏工作台、260px 固定侧栏、无顶部栏、品牌区、白灰黑 token、XEPT 圆角、按钮、输入、选择控件、Slider、知识树、代码框、卡片、动效、总览 5 卡结构、空数据/已有题目双状态和禁止恢复项。
- `AGENTS.md` 已增加强制规则：后续任何前端 UI 任务必须先阅读该最终模板，默认只实现样式，不新增业务逻辑。
- 参考图：`docs/design/reference/overview-empty.png` 与 `overview-with-problem.png`。

## 2026-09-14 知识树页面定稿

- 知识树页面已完成双栏改造：左侧标签词表与本地检索，右侧知识点详情。
- 左栏实现 L1/L2/L3 整行色阶、CSS 箭头、默认展开 L1、层级筛选与展开/折叠；搜索结果显示完整路径。
- 右栏区分“可作题目标签”和“只用于统计，不作为标签”，并保留学习目标与推荐入口。
- 1440×900、1280×800 主体无滚动；1024px 无横向溢出；控制台无错误；搜索、键盘和 reduced-motion 已浏览器验证。

## 2026-09-14 知识树 OI Wiki 风格修订（最新覆盖）

- 用户要求知识树改为 OI Wiki 风格：顶部放全部 L1，默认基础算法，搜索框在最右侧。
- 页面标题、词表信息、领域说明和筛选按钮全部删除，左侧只显示当前 L1 的 L2/L3。
- 树条目统一白底、无等级标签；L3 左端、箭头和圆点整体缩进。
- 右侧删除等级、英文 ID 和用途标签；L2 只保留下级节点，下级节点用横线列表而不是卡片。
- 这是对上一版知识树布局的覆盖性要求，后续不得恢复黑/灰/白分级行和等级框。

### 2026-09-14 知识树叶子与下级列表修正

- L2/L3 叶子节点都要显示知识点介绍，不能只按层级判断。
- 分支节点不再显示“下级节点”标题，也不显示标题与列表之间的顶部横线。
- 下级列表直接铺开，仅由各行之间的横线区分。

### 2026-09-14 L1 详情路径修正

- L1 详情不显示路径导航，L2/L3 继续显示可点击路径。
- 前端读取祖先路径时必须使用 `node.ancestors || []`，防止缺失字段导致 `not iterable`。

### 2026-09-14 详情字段边界修复

- `Cannot read properties of undefined (reading 'length')` 的根因是详情渲染直接读取 `node.children.length`。
- 详情对象必须在渲染边界统一规范化 `children` 和 `ancestors`，不能假设接口或缓存对象字段完整。

### 2026-09-14 search 节点路由冲突

- 知识节点 ID `search` 曾与 `/api/knowledge/search` 搜索接口冲突，导致右侧详情为空。
- 详情查询现统一使用 `/api/knowledge/node/{id}`；新增节点 ID 不应再与保留接口路径共享命名空间。

### 2026-09-14 L1 知识插画定稿

- 9 个 L1 使用代码原生黑色 SVG 线条插画，不用 GPT 位图或外部图标。
- L1 详情不显示下级目录，只显示标题和插画；L2/L3 保持原详情逻辑。
- 主题分别为等距立体积木、八皇后棋盘、六级实体 DP 台阶、共享前缀字典树、勾股定理证明图、单棵平衡红黑树、最短路径、凸包、数组双指针。

## 2026-09-17 知识体系 v2026.09.8 标签清理

- 用户要求删除不适合作为题目标签的 Wiki 章节式节点，并拆分、重命名、提升、新增部分知识点。
- 当前版本为 2026.09.8，共 347 节点：9 L1 / 153 L2 / 185 L3，315 个叶子全部带 summary。
- 后续仍建议复核布尔代数、坐标系、序理论、计算几何杂项、空间优化、PQ 树、手指树等偏理论或偏冷门节点，但应结合真实题目标注反馈决定。

## 2026-09-17 知识体系 v2026.09.9 后续清理

- 当前版本 2026.09.9，共 345 节点：9 L1 / 151 L2 / 185 L3，313 个叶子全部带 summary。
- 删除弧度制与坐标系、空间优化两个独立标签；坐标系与弧度信息并入二维计算几何介绍。
- “数字系统”改为“进位制”，其原同名子节点改为“进制转换”。

### 2026-09-17 知识点中英文搜索

- 搜索同时匹配中文名和英文 ID，不区分大小写。
- 空格、点、横线、斜杠、圆括号等分隔符统一归一化，因此 `shortest path` 与 `graph.shortest-path` 可互相匹配。
- 前端仍使用缓存树本地搜索，避免每次输入请求后端。

### 2026-09-17 知识树详情按钮移除

- 知识树详情只负责浏览和检索，不再显示“设为学习目标”“看推荐训练”按钮。
- 相关页面级 CSS 和导航依赖同步删除。
