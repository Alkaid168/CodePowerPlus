# 项目发现

## 2026-09-13 初步发现
- 最初启动时仓库为空（历史记录，当前已有早期原型）。
- 项目名称：码力加加程序设计智能辅导系统；团队王禹凡、廖致涵、廖益博，指导教师黄华。
- 目标：为传统 OJ 增加题目解析、用户画像、错误代码辅导、题目推荐/学习路径。
- 数据不必依赖学校 OJ 源码：可先使用 Codeforces/洛谷等公开题目与提交数据，后续通过 API/导出数据接入学校 OJ。
- 研究点：知识追踪、最近发展区、结构化能力/题目知识图谱；工程点：前后端分离、LLM Agent、JSON 输出、数据库、可视化。
- 现有测试实验.py 使用 DeepSeek API，根据题面输出 tags 与 CF 难度。
- 立项计划原定 2026-06 至 2027-06，当前已滞后，需要重排。


## 用户确认
- 可使用 DeepSeek API。
- 目标优先级：中期答辩前完成可运行的最小规模原型；后半期改进算法、提示词、模型和知识体系。
- 无学校 OJ 特权、源码或数据，只能普通用户访问。
- 架构必须与学校 OJ 解耦，先支持公开数据/手工导入，未来再增加适配器。


## 2026-09-13 工作量评估
- 当前代码已覆盖功能骨架，但距离申请书的研究承诺仍有较大差距；不能仅以接口数量作为项目完成度。
- 申请书四项核心内容：题目解析智能体、用户画像/能力评估、智能辅导、题目推荐/学习路径；研究方案还承诺知识库、知识追踪、AST+LLM、公开题库数据、问卷与对比实验。
- 后续必须增加数据集与标注规范、taxonomy 维护、模型/提示词对比、能力模型对比、推荐评估、辅导质量评估、用户实验和可复现报告。


## 2026-09-13 知识体系占位节点
- “把目录一次扩充到几百个节点”这个目标本身会诱使生成的节点只有名字没有内容：v2026.09.4 就是这样产生的 301 个占位节点。
- 判别特征：L2 名称是英文单词（Io、Stl、Optimization），每个分支固定挂 6 个“Xxx专题 N”，定义统一为“X的核心概念、性质与实现方法。”，典型算法就是节点名本身。
- 这类节点不能用于题目标注：AI 选出的标签会指向没有语义的知识点，画像和推荐也会跟着失真。
- 结论：目录规模不是指标，可标注性才是。v2026.09.5 用 304 个逐条自编节点替换，并新增测试防止模板节点再次混入。


## 2026-09-13 标签词表才是知识体系的正确形态
- 知识体系的用途是给题目打标签，节点必须能被回答“这道题考什么”。因此“定义/难度/典型算法/学习目标/前置关系”都不属于这里，它们把标签词表变成了教材。
- 判断一个节点该不该存在，只问一句：会有题目被标上它吗？“编译与调试”“C++ 现代特性”“变量”“文件操作”都过不了这个问题。
- 层级不必整除：二级既可以是分支（数学 → 数论 → 素数），也可以是知识点本身（数学 → 快速幂）。OI Wiki 的栏目导航天然就是这个形状，可直接作为规模与粒度的参照。
- 去掉前置关系后，推荐只剩“标签薄弱度 + 难度适配”，学习路径退化为知识树层级路径；评测不再统计前置违例。这是简化，也要在文档和实验局限里写清楚。
- OI Wiki 的粒度是“一页一个主题”：最短路、背包 DP 这类页面内部包含多个算法，所以标签粒度也停在这一层；需要更细时按题目需求再加 L3。


## 2026-09-13 靠分层把“能改”变成“改得起”
- 旧代码的核心问题不是“功能少”，而是**职责混在一起**：接口、业务、SQL 挤进 `main.py` 和仓储类，改一处要通读全部；前端 32KB 压缩单文件同理。
- 重写后新增功能只需要动一处：新规则进 `services/`，新接口进 `api/`，新表进 `db.py` 加迁移。101 项测试给了这条路径安全网。
- 依赖注入（`Container`）不是为了“设计模式好看”，而是让测试能给每个用例一套独立数据库，避免用例互相污染，也不用改全局变量。
- 前端不引入 npm 构建是**有代价的选择**：少了组件生态，换来“一条命令跑起来”和可读的源码；对单用户原型更划算。
- 用 `h()` 建节点替代字符串拼 HTML，是安全性和可维护性一起收益的改动：文本不再有机会变成标签或脚本。


## 2026-09-13 UI 模板库与实现边界

- 视觉规范如果不把“默认态”和“交互态”明确分开，设计稿会把 focus 样式误当成默认样式，导致输入框边框颜色看起来不一致。
- Xept 的可复用价值主要是控件尺度、边界和紧凑信息密度，不是品牌外观；本项目最终采用黑白灰主色和无渐变。
- 结果状态需要固定右边界、内容自适应宽度和居中文字，不能使用固定宽度拉伸。
- 知识树的层级表达应作用于整行：L1 黑、L2 中灰、L3 浅灰，箭头用 CSS 绘制，避免字体导致错位。
- 组件规范与页面布局必须分阶段确认。当前 `docs/design/` 是组件层事实来源，`app/static` 仍是旧视觉实现，不代表目标设计。

## 2026-09-14 总览实现经验

- 全局 token 可以先统一视觉，但页面布局不要跟着组件一起重写；总览可以单独新增 `overview.css`，其他页面先继承 token，降低回归面。
- 浏览器缓存会掩盖静态 CSS 修改，前端自检必须禁用缓存或加版本参数，否则会把旧样式误判为新结果。
- 静态数据卡不应因为 hover 而位移；总览只有按钮和可操作入口使用轻微位移，卡片仅改变边界。
- 输入端默认灰边、聚焦黑边、白底、禁用灰底必须在计算样式层验证，不能只看缩略图。

## 2026-09-14 全局模板细节

- 品牌资源应先做白底去除与裁切，不直接使用原图；透明 PNG 才能适配不同白色/灰色表面。
- 英文艺术字不能阻塞页面结构实现，先用 CSS 变量占位，字体商定后只替换变量。
- 固定顶栏和独立主体滚动是布局职责，不应靠页面自身滚动修补。
- 卡片入场动效应作用于组件层，保证所有页面复用；减少动态时只保留透明度反馈。
- “减少小字”不是删除信息层级，而是把重要数值和标题放大、把解释性说明移到需要时再展示。

## 2026-09-14 无顶栏与平铺布局（历史阶段，已被白卡版本覆盖）

- 顶部栏会重复占用纵向空间，在桌面工作台中可以删掉，把页面标题并回主体首行。
- 去除卡片外壳后，层级应由字号、留白和细线承担；不要把背景容器换一种形式重新包回去。
- 页面级滚动必须由主体容器承担，不能通过隐藏 overflow 掩盖内容问题。

## 2026-09-14 XEPT 轻量白卡经验

- XEPT 的可借用点是白底、清晰黑字、清晰动作边界和留白；总览主卡不需要额外的右侧灰色小卡或重复 Logo。
- 黑色面积一旦超过标题和按钮级别，就会压过内容层级；白色内容面配合足够留白已经能保留主次，不需要再加内嵌卡片。
- 用户只要求取消黑色主卡，不代表按钮也要去黑化；Primary 仍应保持黑底白字，黑色小面积动作与白色内容面可以同时成立。
- 总览卡片继续使用 12px 圆角，不跟随大屏放大圆角；与已确认的 XEPT 控件比例保持一致。

## 2026-09-14 前端模板的唯一事实源

- 设计历史分散在多个阶段记录里会产生冲突；最终版要求必须收敛到 `docs/design/FINAL_FRONTEND_TEMPLATE.md`。
- 该文件明确区分最终规则、已废弃结构和待确认项，后续对话不需要再从聊天记录反向推断。
- `AGENTS.md` 是自动入口，最终模板是设计事实源；两者组合后，新对话会在前端任务开始前读取正确基线。

## 2026-09-14 知识树页面实现发现

- 通过给知识树路由挂载专用外壳类，才能让 `main` 保持固定可视高度并在左右两栏内部滚动；仅给页面容器设置高度会被 flex 内容撑开。
- 382 个节点可以一次性在浏览器渲染；默认只展开 L1，用户主动展开全部时页面仍可正常操作。
- 搜索必须使用缓存全树本地过滤，否则输入即搜会制造无意义的网络请求；当前“最短路”命中 2 条，路径完整可读。
- 浏览器存在缓存时，静态 JS 修改可能被旧文件掩盖；布局验收必须在修改后强制刷新并读取计算样式。
- reduced-motion 下车点动画被关闭，面板只保留透明度动画。


## 2026-09-14 CC Switch 路由导致 DeepSeek 工具调用 400

- 故障现象：CC Switch 本地路由开启时，Codex 会话报 `No tool output found for tool call ...`，后续消息持续失败，任务进入 `systemError`。
- 实际会话数据没有丢失：本地 rollout 中该 `call_id` 的 `function_call` 和 `function_call_output` 都存在且配对正确。
- 根因是同一轮并行工具调用中，`view_image` 返回图片后 Codex 插入了 `<image_resize_notice>`；CC Switch v3.20.3 的 Responses→Chat 历史转换会把图片结果提前搬成合成 `user` 消息，导致部分 `function_call_output` 与对应调用被拆开。DeepSeek 严格校验后返回 HTTP 400。上游 issue 为 farion1231/cc-switch#7190，截至 v3.20.3 未修复。
- 可复现条件：CC Switch 本地路由开启；同一模型轮次至少包含图片工具调用和另一个工具调用；图片触发了缩放通知。
- 规避方案：使用 DeepSeek 官方 Responses 接口时关闭 CC Switch 本地路由，走 Codex 直连；避免把 `view_image` 与其他工具放在同一批并行调用。

## 2026-09-14 知识树 OI Wiki 风格修订发现

- OI Wiki 式顶部 L1 导航比层级筛选按钮更直接；当前领域切换后，左侧树与右侧详情应同步更新。
- 左侧只渲染当前 L1 子树可以显著降低浏览噪音，同时保留顶部全网搜索的快速跳转能力。
- 白色树条目必须依赖边框、选中侧线和留白建立层级，不能重新用背景色制造区分。
- 用户要求 L3 整条缩进时，应同时移动条形容器、箭头和圆点，而不是只给文字加 margin。
- L2 的“没有单独介绍”没有信息价值；详情面板应当直接展示下级节点。

### 2026-09-14 知识树叶子与下级列表修正

- “有下级”和“属于 L2”不是同一条件；叶子判断应以 `children.length` 为准。
- 分支详情中的“下级节点”标题和顶部横线重复制造层级，直接列表搭配行分隔线更清晰。

### 2026-09-14 L1 详情路径报错

- 根因是详情渲染无条件展开 `node.ancestors`，但 L1 业务上不需要路径，且应防御缺失字段。
- 修复采用 L1 特判加 `node.ancestors || []`，同时避免错误和满足无路径 UI 要求。

### 2026-09-14 children 缺失导致 length 报错

- 第二次报错的直接位置是 `Boolean(node.children.length)`；只给 `ancestors` 做兜底不够。
- 修复应在详情对象边界统一规范化 `ancestors` 和 `children`，而不是在每个 `.length` / `.map` 调用点零散判断。
- 已用浏览器拦截详情响应并删除两个字段：L1 与 L2 叶子均正常渲染，控制台 0 错误。

### 2026-09-14 L1 search 右侧空白

- 根因不是渲染异常，而是路由冲突：动态详情 `/api/knowledge/{id}` 与保留搜索接口 `/api/knowledge/search` 重名。
- 节点 `search` 的详情请求被搜索接口截获，返回 `version/total/items`，因此右侧卡片没有名称、层级和子节点。
- 修复方案是新增独立详情命名空间 `/api/knowledge/node/{id}`，前端详情统一走该路径；旧动态路由保留兼容。
- 已逐个请求全部 382 个节点的专用详情接口，返回结构错误数为 0。

### 2026-09-14 L1 知识插画实现

- 简单几何概念图适合代码原生 SVG，不需要 GPT 位图；最终资产可控、清晰且没有网络依赖。
- L1 详情不需要重复展示下级目录，插画能更快表达领域核心问题。
- 红黑树最终只画一棵 1–7 的平衡树，不再用旋转箭头；用户要的是树结构本身。
- 字典树必须用共享前缀和终止节点才能体现结构，不能画成规则二叉树。
- 双指针比瑞士军刀更容易让算法竞赛用户立即理解“杂项”中的经典技巧。

## 2026-09-17 知识体系标签审计

- 题目标签应当是“题面或解法中可被指认的考点”，不能只是 OI Wiki 的目录章节名。
- 本轮已删除复杂度、搜索优化、动态规划基础、抽象代数、概率论/博弈论子目录、多种计算理论/工具基础，以及一批竞赛中通常作为实现细节而非独立标签的项目。
- 额外审查建议优先复核：`math.boolean-algebra`、`math.coordinate`、`math.order-theory`、`geometry.misc`、`misc.space-optimization`、`ds.pq-tree`、`ds.finger-tree`。
- 这些候选不应直接批量删除，需要结合以后真实题目标注数据判断；其中“布尔代数”更适合作为位运算/逻辑模型的一部分，“空间优化”更适合作为具体 DP 转移方案的证据。

## 2026-09-17 知识体系 v2026.09.9 后续审查

- `math.coordinate` 的弧度制和坐标系属于计算几何的基础表达，已删除并合并进 `geometry.2d` 介绍。
- `math.numeral-system` 已改成更明确的“进位制”；原同名子节点改名为“进制转换”，避免重复和空泛。
- `misc.space-optimization` 属于具体实现优化，不足以成为独立题目标签，已删除。
- 后续仍需结合真实题目判断布尔代数、序理论、计算几何杂项、PQ 树和手指树等候选。

### 2026-09-17 知识点中英文搜索

- 仅对名称和 ID 直接做 substring 会漏掉空格、横线、点号写法差异，例如 `shortest path` 与 `graph.shortest-path`。
- 搜索前统一转小写并把分隔符归一化为空格，可同时支持中文名、英文 ID、括号英文和大小写变化。
- 搜索结果为包含匹配，因此 `stack` 同时返回栈与单调栈、`KMP` 同时返回 KMP 与扩展 KMP，属于合理召回。

### 2026-09-17 知识树详情动作按钮移除

- 知识树承担浏览和检索职责，详情中的“设为学习目标”“看推荐训练”会重复学习路径与推荐页入口，已按要求删除。
- 删除应同时清理组件结构、按钮文案和页面级 CSS，避免留下不可见布局残留。

## 2026-09-21 深链接必须驱动整页状态

- 跳进知识树的入口有三个：能力画像、推荐训练、学习路径里的知识点按钮都指向 `#/knowledge/<id>`。
- 只渲染详情会让右栏显示目标知识点、左栏停在第一个领域，看起来像跳错了页面。
- 正确做法是让 URL 参数走与点击节点完全相同的那条路径：切换 L1 → 展开祖先 → 高亮当前行 → 渲染详情。
- 浏览器缓存会掩盖前端修复：静态资源没有 `Cache-Control`，模块会被启发式缓存；验证前要重开浏览器上下文或加版本参数。